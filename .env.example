# e-jadwal (admin) environment variables
MONGO_URI=mongodb://localhost:27017/ejadwal_admin
PORT=3000
# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=http://localhost:4000
# Session secret (change this to a strong random string in production)
SESSION_SECRET=change-me-in-production
# Node environment
NODE_ENV=development
# Basic Auth credentials to protect /admin and /api
ADMIN_USER=
ADMIN_PASS=
